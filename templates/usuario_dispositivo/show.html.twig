{% extends 'base.html.twig' %}

{% block title %}UsuarioDispositivo{% endblock %}

{% block body %}
    <h1>UsuarioDispositivo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ usuario_dispositivo.id }}</td>
            </tr>
            <tr>
                <th>Id_dispositivo</th>
                <td>{{ usuario_dispositivo.idDispositivo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('usuario_dispositivo_index') }}">back to list</a>

    <a href="{{ path('usuario_dispositivo_edit', {'id': usuario_dispositivo.id}) }}">edit</a>

    {{ include('usuario_dispositivo/_delete_form.html.twig') }}
{% endblock %}
